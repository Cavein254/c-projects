#include <stdio.h>
#include <stdlib.h>

typedef struct {
    int data;
    Node *next;
} Node;

int main ()
{
    Node aNode;
    Node bNode;
    Node cNode;
    Node dNode;

    aNode = (Node*)malloc(sizeof(Node));
}